# Claim 2 route B evaluation

Pending the formal OpenResearch run.

The only allowed route results are `FALSIFIED` if a valid probability-level
counterexample is established, or `NO_VALID_COUNTEREXAMPLE_FOUND`. A finite
search with no counterexample is not labeled verification.
