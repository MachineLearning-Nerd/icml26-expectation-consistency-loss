# Publication record — ECL covariate-shift reproduction

The approved additive release was published to the existing Hugging Face
Space `DineshAI/gFPPTokv9C` at revision
`389efd99d6e4fc28a90ec7aaddcbecec467f01d0`.

The downloaded public tree passes the release verifier:

- all 26 original judged paths remain present;
- all old pages are byte-identical;
- `logbook.json` is the only modified old path;
- five additive claim pages are reachable;
- all 98 uploaded paths are UTF-8 text;
- the manifest, JSON parsing, and secret scan pass.

The first upload commit was followed by a four-file bookkeeping correction:
the local custody file `PROTECTED_MANIFEST.sha256` had initially been counted
as an original remote path. It was never part of the judged Space and was
never uploaded. The correction changed the reported path counts from 27/124
to the actual 26/123; it did not change evidence, verdicts, or the upload
allowlist.

GitHub `master` contains the illustrated report, the validated marimo
notebook, the complete reproduction implementation, and byte-identical mirrors
of all 98 Space text paths.

Status: **AWAITING_JUDGE**. The latest live judge record still references
Space revision `b864c4b287cffb41d35d51e471f0f23013a787e4` and score `6/12`.
No score increase is claimed.
