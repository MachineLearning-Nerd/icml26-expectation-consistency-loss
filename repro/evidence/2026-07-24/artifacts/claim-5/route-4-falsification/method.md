# Claim 5 route 4 method

For every compared Table 2 cell, the primary audit constructs ten bounded ECE
observations with exactly the printed mean and sample standard deviation. This
does not verify the paper; it tests whether the summaries are internally
impossible. It then evaluates all three completed experimental routes against
the necessary conditions for a valid counterexample and verifies the source
audit's protocol blockers.

A separate standard-library checker recomputes all six means and sample
standard deviations, verifies all observations lie in [0, 100], confirms that
exactly three prior routes were rejected for stated assumption failures, and
requires an exact BLOCKED result when no valid counterexample exists.
